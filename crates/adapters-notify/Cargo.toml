[package]
name = "lindex-adapters-notify"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

# Driven adapter: failure alerting transports (log line, webhook). Implements the
# application's `Alerter` port. reqwest/serde_json are already workspace deps.
[dependencies]
lindex-application = { path = "../application" }
async-trait.workspace = true
reqwest.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio.workspace = true

[lints]
workspace = true
