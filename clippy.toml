# Intentionally empty. Lint *levels* live in [workspace.lints] in Cargo.toml and
# are enforced by `cargo clippy -- -D warnings` (see justfile / CI). Add a
# concrete clippy configuration here only when a specific lint needs tuning,
# with an ADR justifying it.
