[package]
name = "ingest"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

# Composition root: the only place adapters meet ports.
[dependencies]
lindex-domain = { path = "../../crates/domain" }
lindex-application = { path = "../../crates/application" }
lindex-adapters-persistence = { path = "../../crates/adapters-persistence" }
lindex-adapters-sources = { path = "../../crates/adapters-sources" }
lindex-adapters-notify = { path = "../../crates/adapters-notify" }
tokio.workspace = true
anyhow.workspace = true
sqlx.workspace = true
chrono.workspace = true

[lints]
workspace = true
