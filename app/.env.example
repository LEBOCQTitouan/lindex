# Read-only credentials against the facts schema (owned by the Rust plane).
DATABASE_URL_FACTS=postgres://lindex_reader:password@localhost:5432/lindex

# App-owned writes (accounts, follows, editorial review).
DATABASE_URL_APP=postgres://lindex_app:password@localhost:5432/lindex
