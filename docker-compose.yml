services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: lindex
      POSTGRES_PASSWORD: password
      POSTGRES_DB: lindex
    ports:
      - "5432:5432"
    volumes:
      - lindex_pg:/var/lib/postgresql/data

volumes:
  lindex_pg:
